#include "main.h"

/**
 * _islower - Check if it is in lowercase.
 * _islower - write the lowercase
 *
 * Return: Always 0.
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

		_putchar('\n');
}
