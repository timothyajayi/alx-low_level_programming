#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - Entry Point
 * @mul: is a function that converts a string into an int
 * @a: arguments
 * @b: array pointing to arguments
 * Return: 0
 */
int mul(int a, int b)
{
	return (a * b);
}
