#include <stdio.h>
/**
 * main - entry point
 * @argc: number of arguments typed
 * @argv: array pointing to arguements
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d", argc);
	printf("%s\n", argv[0]);
	return (0);
}
